# estat-api-data-search

This plugin captures the discovery stage for machine-readable e-Stat tables.

The current scaffold intentionally avoids Python wrappers until the target workflow is specified in
more detail.

# estat-file-search

This plugin stays documentation-first for now.

It provides:

- `SKILL.md` for activation rules
- `reference.md` for endpoint mapping and operating notes
- `recipes/` for reusable curl/xh examples
